import { SxProps } from "@mui/material";

export const style: { typography: SxProps } = {
  typography: {
    position: 'absolute',
    marginTop: '12rem',
    flexWrap: 'wrap',
    textAlign: 'center',
    fontSize: '1.15rem',
    color: '#FFFFFF'
  }
}